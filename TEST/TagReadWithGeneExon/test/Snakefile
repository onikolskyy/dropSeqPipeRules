rule test:
    input:
        # define input paths
        refflat='/input/annotation.refFlat',
        inbam='/input/in.bam',
        correctbam='/input/correct.bam'
    output:
        # define output path
        outbam='out.bam'
    conda: 'envs/tag_read_with_gene_exon.yaml'
    benchmark : 'benchmark.txt'
    script: 'scripts/test.py'
